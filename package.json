{
  "name": "email-extraction-cmh9",
  "version": "7.0.0",
  "private": true,
  "description": "Production Gmail IMAP extractor with mode-specific MIME output and safe header transformations",
  "main": "server.js",
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check lib/email-engine.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "imapflow": "^1.0.181",
    "mailparser": "^3.7.4"
  }
}
