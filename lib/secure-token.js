'use strict';
const crypto=require('crypto');
function keyFromSecret(secret){if(!secret||String(secret).length<32)throw new Error('SESSION_SECRET must contain at least 32 characters');return crypto.createHash('sha256').update(String(secret)).digest()}
function sealCredentials(credentials,secret){const iv=crypto.randomBytes(12);const cipher=crypto.createCipheriv('aes-256-gcm',keyFromSecret(secret),iv);const plaintext=Buffer.from(JSON.stringify(credentials),'utf8');const ciphertext=Buffer.concat([cipher.update(plaintext),cipher.final()]);return [iv,cipher.getAuthTag(),ciphertext].map(value=>value.toString('base64url')).join('.')}
function openCredentials(token,secret){try{const parts=String(token||'').split('.');if(parts.length!==3)return null;const[iv,tag,ciphertext]=parts.map(value=>Buffer.from(value,'base64url'));const decipher=crypto.createDecipheriv('aes-256-gcm',keyFromSecret(secret),iv);decipher.setAuthTag(tag);const payload=JSON.parse(Buffer.concat([decipher.update(ciphertext),decipher.final()]).toString('utf8'));if(!payload||typeof payload.email!=='string'||typeof payload.password!=='string')return null;return{email:payload.email,password:payload.password}}catch{return null}}
module.exports={sealCredentials,openCredentials};
